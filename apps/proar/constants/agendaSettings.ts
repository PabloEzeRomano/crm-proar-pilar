// AsyncStorage key for the "group by client" agenda preference. Shared
// between the agenda screen (reads it) and Settings (writes it).
export const AGENDA_GROUP_BY_CLIENT_KEY = 'agenda-group-by-client';
