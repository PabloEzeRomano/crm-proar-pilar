/**
 * app/(tabs)/clients/visits/form.tsx — Visit form (Clients stack)
 *
 * Re-exports the shared form so that creating/editing a visit from
 * the Clients tab stays within the Clients stack.
 */

export { default } from '../../visits/form';
