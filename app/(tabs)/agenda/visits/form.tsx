/**
 * app/(tabs)/agenda/visits/form.tsx — Edit visit form (Agenda stack)
 *
 * Re-exports the shared form so that "Editar" from the Agenda detail
 * stays within the Agenda stack and back navigation works correctly.
 */

export { default } from '../../visits/form';
