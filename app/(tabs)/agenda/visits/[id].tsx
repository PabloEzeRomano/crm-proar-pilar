/**
 * app/(tabs)/index/visits/[id].tsx — Visit detail (Agenda stack)
 *
 * EP-022: Visit detail screen inside the Agenda stack.
 * Back button navigates naturally to the Today dashboard in this stack.
 * No query-param workaround needed.
 */

export { default } from '@/components/visits/VisitDetailView';
